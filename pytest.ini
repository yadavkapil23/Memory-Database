[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto

# Show print output
addopts = -v -s

# Markers
markers =
    unit: Unit tests
    integration: Integration tests
    slow: Slow tests
